# **01 \- Coding Standards & Conventions**

**Project Name:** \[Project Name\]

**Generated By:** @tech-lead

**Reference:** docs/01-system-context/01-Architecture.md, docs/01-system-context/02-Database.md

## **1\. Codebase Architecture & Folder Structure**

\<\!-- AI INSTRUCTION: Based on the tech stack defined in 01-Architecture.md, strictly define the allowed directory structure using the table below. Specify exactly where components, utilities, API routes, and services must be placed. \--\>

* **Framework Pattern:** \[e.g., Next.js App Router conventions / NestJS Modular architecture\]  
* **Strict Rule:** \[e.g., DO NOT place business logic directly inside UI components. Always extract them to custom hooks or service files.\]

**Allowed Directory Structure:**

| Directory Path | Purpose / Allowed Content |
| :---- | :---- |
| src/components/ui/ | \[e.g., For dumb/stateless reusable UI components ONLY.\] |
| src/features/\[feature\]/ | \[e.g., For smart components and business logic tied to a specific feature.\] |
| src/lib/ | \[e.g., For third-party library initializations like Axios or Prisma Client.\] |
| src/utils/ | \[e.g., For pure helper functions (formatting dates, calculations) without side effects.\] |

## **2\. Naming Conventions**

\<\!-- AI INSTRUCTION: Define the exact casing rules for files, variables, classes, and database models to ensure 100% consistency across the codebase. Fill in the table below. \--\>

| Category | Element Type | Casing Rule / Convention |
| :---- | :---- | :---- |
| **Files & Directories** | React Components | \[e.g., PascalCase.tsx\] |
|  | Utility Functions | \[e.g., camelCase.ts\] |
|  | General Folders | \[e.g., kebab-case\] |
| **Variables & Functions** | Standard Variables | \[e.g., camelCase\] |
|  | Constants | \[e.g., UPPER\_SNAKE\_CASE\] |
|  | Boolean Variables | \[e.g., Prefix with is, has, or should (e.g., isVisible, hasAccess)\] |
| **Classes & Types** | Classes | \[e.g., PascalCase\] |
|  | Types/Interfaces | \[e.g., PascalCase, optionally prefix with I or suffix with Type\] |

## **3\. State Management & Data Fetching**

\<\!-- AI INSTRUCTION: Define the standardized approach for managing frontend state and fetching data from the backend. Fill in the strategies in the table below. \--\>

| State Type | Standardized Approach / Library |
| :---- | :---- |
| **Local State** | \[e.g., Use standard React useState. Avoid over-complicating with Redux for simple toggles.\] |
| **Global State** | \[e.g., Use Zustand for client-side global state (e.g., User Theme, Sidebar Toggle).\] |
| **Server State / Fetching** | \[e.g., Strictly use React Query (@tanstack/react-query) or Next.js native fetch. DO NOT use plain useEffect.\] |

## **4\. Error Handling & Logging**

\<\!-- AI INSTRUCTION: Establish the standard pattern for catching and returning errors in both frontend and backend code using the table format. \--\>

| Error Domain | Standard Pattern / Handling Strategy |
| :---- | :---- |
| **Backend Error Throwing** | \[e.g., Always use custom Error classes (e.g., AppError(message, statusCode)). Never throw plain strings.\] |
| **API Error Responses** | \[e.g., All failed API responses MUST match the JSON structure defined in 03-API-Specs.md.\] |
| **Frontend Error Catching** | \[e.g., Wrap critical components in Error Boundaries. Use try/catch blocks and trigger a global Toast notification.\] |

## **5\. Formatting & Linting Rules**

\<\!-- AI INSTRUCTION: Set the baseline rules for code formatting to simulate Prettier/ESLint constraints for the AI agents. \--\>

| Rule Category | Standard / Enforcement |
| :---- | :---- |
| **Quotes** | \[e.g., Prefer single quotes (') for JS/TS, double quotes (") for JSX/HTML attributes.\] |
| **Semicolons** | \[e.g., Always required at the end of statements.\] |
| **Indentation** | \[e.g., 2 spaces.\] |
| **Type Safety** | \[e.g., Strict TypeScript mode is ON. The use of any is strictly FORBIDDEN. Use unknown and validate.\] |

